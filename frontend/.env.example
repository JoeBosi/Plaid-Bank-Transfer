# Frontend dev server port (default 4000 if not set in package.json)
PORT=4000

# Backend API base URL
REACT_APP_API_URL=http://localhost:4001

# Sentry DSN (optional)
REACT_APP_SENTRY_DSN=
REACT_APP_ENV=development
